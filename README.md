# receiptify
Receiptify is a FastAPI-based app that collects student details via a simple frontend, generates a PDF receipt from a .docx template, and instantly emails it. It automates registrations, saves records in JSON/YAML/Excel, and provides institutions a fast, paperless solution.
